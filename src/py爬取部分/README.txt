上述文件中最后一版为GetReviews.py（因为短评太多爬不完，所以转而爬取长评论），之后使用DealWithText进行预处理（处理成为xml格式，重要的内容（也就是用户名和他们的评价）放在其中，省略其余部分）
CreateFile.py可以自动生成程序之中所用到的四个文件：助教提供的movie.csv、user.csv、最后我使用的数据文件out.tsv和value.tsv
SpiderForDouBan.py为第一版的（爬取所有短评），而ChangeIP.py是用于更换代理服务器IP的（使用网上一个免费的代理服务器，但是效果并不好，所以最后未采用此方法）
这些文件之中，包含了断点续爬的内容（可以通过更改开启位置来进行调节，因为爬取过程中可能需要重启），在里面也包含服务器版本和Windows版本两个版本